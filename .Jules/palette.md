## 2024-05-22 - Missing Tooltips on Icon Buttons
**Learning:** Icon-only buttons (like those in AppBars) are inaccessible to screen readers and confusing for mouse users without tooltips.
**Action:** Always add `tooltip` property to `IconButton` and `FloatingActionButton` to provide an accessible label and visual hint.
